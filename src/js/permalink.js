


if (window.location.hash !== '') {
}
/*
var center = view.getCenter();
var hash = '#map=' +
  view.getZoom() + '/' +
  Math.round(center[0] * 100) / 100 + '/' +
  Math.round(center[1] * 100) / 100 + '/' +
  view.getRotation();

const permalinkSet = "set"
*/

export function permalinkGet(x) {
}

